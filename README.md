# Smart Route Visualizer
Python project to find optimal routes using multiple algorithms like BFS, DFS, A*, Hill Climbing, and Best-First Search.

## How to Run
Run the main script `AItproject.py` using Python 3.x:
```bash
python AItproject.py
